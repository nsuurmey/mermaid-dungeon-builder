; better-sqlite3 has no prebuilt binary for every Node ABI, and some sandboxed
; build environments block the GitHub prebuild download. Building from source
; keeps installs deterministic anywhere a C++ toolchain is present (incl. Railway).
build_from_source=true
